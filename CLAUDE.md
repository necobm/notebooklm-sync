# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**`AGENTS.md` is the master document for this repo — read it.** Commands, architecture, the
invariants, upstream CLI quirks, the auth model, testing, and the required engram memory workflow
all live there.

@AGENTS.md

---

**This file holds references only.** Anything durable about how this repo works belongs in
`AGENTS.md`, never here — do not copy a section over, do not add a "quick reference", do not let
this file grow. Two copies of a rule means one of them is already wrong. If you learn something
worth writing down, add it to `AGENTS.md` (and to engram, per its Memory section).
